{
  "name": "veda-client",
  "version": "3.0.0",
  "description": "veda platform client framework",
  "module": "src/index.js",
  "main": "src/index.js",
  "types": "src/index.d.ts",
  "type": "module",
  "scripts": {
    "test": "tsc --noEmit && time -p node --expose-gc test",
    "test:unit": "tsc --noEmit && time -p node --expose-gc test --unit",
    "test:integration": "tsc --noEmit && time -p node --expose-gc test --integration",
    "test:benchmark": "time -p node --expose-gc test --benchmark",
    "debug": "node --inspect-brk test",
    "c8": "tsc --noEmit && time -p c8 node --expose-gc test",
    "c8:unit": "tsc --noEmit && time -p c8 node --expose-gc test --unit",
    "build": "node build.mjs",
    "build:devtools": "node devtools/build.js",
    "watch": "node watch.mjs",
    "typecheck": "tsc --noEmit"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/semantic-machines/veda-client.git"
  },
  "keywords": [
    "reactive",
    "web-components",
    "semantic-web",
    "rdf",
    "veda",
    "framework"
  ],
  "author": "Roman Karpov",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/semantic-machines/veda-client/issues"
  },
  "homepage": "https://github.com/semantic-machines/veda-client#readme",
  "devDependencies": {
    "@eslint/js": "^9.37.0",
    "baretest": "^2.0.0",
    "c8": "^10.1.3",
    "esbuild": "^0.24.2",
    "esbuild-plugin-copy": "^2.1.1",
    "eslint": "^9.37.0",
    "eslint-plugin-sonarjs": "3.0.5",
    "fast-check": "^3.23.1",
    "globals": "^15.15.0",
    "jsdom": "^27.1.0",
    "linkedom": "^0.18.12",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "ws": "^8.18.3"
  }
}
